import { styled, AppBar, Typography, Button } from "@mui/material";

export const MyAppBar = styled(AppBar)`
  background: #000;
`;

export const Login = styled(Button)`
  margin-right: 2px;
  font-family: monospace;
  font-weight: 200;
  color: white;
  text-decoration: none;
`;
